package daemon

import (
	"moby/container"
	"moby/daemon/exec"
	"moby/libcontainerd"
)

func execSetPlatformOpt(c *container.Container, ec *exec.Config, p *libcontainerd.Process) error {
	return nil
}
